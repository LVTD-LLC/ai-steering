{
  "name": "ai-steering",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && node scripts/verify-og-image.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "sync:readme": "node scripts/generate-readme.mjs"
  },
  "dependencies": {
    "astro": "^6.4.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^6.0.3"
  }
}
